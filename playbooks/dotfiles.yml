---
# Idempotent dotfiles-only deploy (no package installation)
- name: Deploy dotfiles
  hosts: local
  gather_facts: true

  roles:
    - role: shell
    - role: git
    - role: tools
    - role: secrets
